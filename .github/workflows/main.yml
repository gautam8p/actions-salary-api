name: salary-action-tests
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: list files
        run: ls -lrth
        
      - name: first-run
        run: mvn clean package
    
